import { ImprovedHighlightDirective } from './improved-highlight.directive';

describe('ImprovedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ImprovedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
